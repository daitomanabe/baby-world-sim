# Contributing

## 原則
- 変更は小さく、PRでレビュー可能にする
- UIに表示する発達情報は **必ず出典と証拠レベル**を付与する
- 断定表現（「赤ちゃんはこう見える」等）を避け、「近似」「代表的傾向」を明示する

## 開発フロー
1. Issue作成（目的・受け入れ条件・出典）
2. ブランチ作成（例：`feat/visual-panel`）
3. 実装
4. `npm run build` が通ること
5. PR作成（変更点、スクショ、出典の追加）

## AI提案を取り込むとき
- AIの提案は**必ずPR**にする（直pushしない）
- 変更点を人間が説明できること（理解できない変更は採用しない）
- 出典リンク/証拠レベルの追加があるか確認
